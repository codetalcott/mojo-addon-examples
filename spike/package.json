{
  "name": "spike-embedding-kernel",
  "version": "0.0.0",
  "private": true,
  "description": "Embedding-kernel spike — sibling to packages/rag, isolated until Day 10 decision.",
  "engines": { "node": ">=22.12" },
  "scripts": {
    "build": "bash build.sh",
    "roundtrip": "node test-roundtrip.js",
    "reference": "node reference.js",
    "bench": "node bench.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.1.0"
  }
}
